# They can't be modified unlike list

tupla = ('hola', 'mundo', 'somos', 'tupla') # We use ( )

# element.index() shows where the element is located
#print(tupla[0])

# You can append stuff in a tuple, we need to transform it to a list

listaDeTupla = list(tupla)
listaDeTupla.append('chanchito')
print(listaDeTupla)

# Ranges
rango = range(6)

print(rango)
